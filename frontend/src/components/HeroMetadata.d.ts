import type { MovieDetail } from '../types';
type Props = {
    movieDetail: MovieDetail;
};
declare const HeroMetadata: ({ movieDetail }: Props) => import("react/jsx-runtime").JSX.Element;
export default HeroMetadata;
