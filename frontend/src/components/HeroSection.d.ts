import '../styles/App.css';
import type { Movie } from '../types';
type Props = {
    movie: Movie;
};
declare const HeroSection: ({ movie }: Props) => import("react/jsx-runtime").JSX.Element;
export default HeroSection;
